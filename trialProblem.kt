fun main(args: Array<String>) {
    var cases = readLine()!!.toInt()
    while(cases > 0){
        val (a, b) = readLine()!!.split(' ')
        println(a.toInt() + a.toInt())
        cases -= 1
    }
}
